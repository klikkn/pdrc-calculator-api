{
  "routes": [
    {
      "method": "POST",
      "path": "/calculate",
      "handler": "Calculate.index",
      "config": {
        "policies": [
          "validate"
        ]
      }
    }
  ]
}